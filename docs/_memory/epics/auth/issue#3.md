## Tarefa: Segurança e Provedor JWT #3

Configurar o ecossistema de segurança para suportar autenticação via Token.

### Critérios de Aceite:
- Criar `JwtService` para geração e validação de tokens.
- Configurar o `SecurityConfig` para proteger as rotas de perfil.
- Permitir acesso público apenas aos endpoints de `/register` e `/login`.